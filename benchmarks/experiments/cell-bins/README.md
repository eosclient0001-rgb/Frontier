# Rejected exploratory compact-bin experiment

Not production code. 32 contiguous IDs per grid cell, with the existing linked list for overflow. The first SwiftShader density diagnostic was slower (17.48 ms vs 15.65 ms); no default change was made. This does not establish native GPU performance. Use the Python profiler's `--source-dir benchmarks/experiments/cell-bins --variant optimized --mode stages` to route these two modules only into the test browser. Raw exploratory result: `../../results/cell-bins-stages.json`.
